# Required for database-backed job creation and reads.
# Relative SQLite paths are resolved from prisma/schema.prisma, so this creates prisma/dev.db.
DATABASE_URL="file:./dev.db"

# Used when generating absolute URLs in future integrations.
APP_BASE_URL="http://localhost:3000"

# Required for real source/mask upload and result storage in Cloudflare R2.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
R2_PUBLIC_BASE_URL=""

# Required for Gemini image editing once the exact API call is implemented.
GOOGLE_GENERATIVE_AI_API_KEY=""
GOOGLE_IMAGE_MODEL="gemini-3.1-flash-image"

# Required for Trigger.dev job dispatch from POST /api/edit-jobs after Prisma creation succeeds.
TRIGGER_SECRET_KEY=""
TRIGGER_API_URL="https://api.trigger.dev"

# Reserved for future realtime delivery wiring.
JOB_EVENTS_WS_URL=""
